.. currentmodule:: discord


Constants
---------

.. data:: MISSING

    The ``MISSING`` object is a special object that is (mostly) used as default value in places where :obj:`None`
    cant be used as it is a valid value too.
    For example in the most ``edit`` functions.